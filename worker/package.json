{
  "name": "gameonpaper-worker",
  "version": "0.0.1",
  "private": true,
  "description": "Cloudflare Worker rewrite of the Express+EJS frontend (Phase 2 of docs/migration-plan.md).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.3",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "hono": "^4.12.16"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.15.2",
    "@cloudflare/workers-types": "^4.20260502.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5",
    "wrangler": "^4.87.0"
  }
}
