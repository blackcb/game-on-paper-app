{
  "name": "sports-python-proxy",
  "version": "0.0.1",
  "private": true,
  "description": "Standalone Worker that fronts python.unseen-university.org and forwards to the sports Worker's PythonContainer DO. See docs/migration-plan.md Phase 3H.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.15.2",
    "@cloudflare/workers-types": "^4.20260502.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5",
    "wrangler": "^4.87.0"
  }
}
