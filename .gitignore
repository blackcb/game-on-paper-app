node_modules
*/node_modules
# Lockfiles intentionally NOT ignored — perf-plan Day 3 added
# frontend/package-lock.json so Playwright + transitives reproduce.
*/.DS_Store
.DS_Store
env
Procfile
.Rproj.user
.Rhistory
.RData
.Ruserdata
cfb-data
cfb-api
.ipynb_checkpoints/*
.ipynb_checkpoints
__pycache__
*/__pycache__

# Playwright runtime artifacts (regenerated on every run)
frontend/test-results/
frontend/playwright-report/
frontend/blob-report/
frontend/.last-run.json
frontend/playwright/.cache/

# Lighthouse CI local artifacts (regenerated on every run; CI uploads
# to temporary-public-storage instead). The .perftest / .prod variants
# are ad-hoc renames done during 3C task #78 to keep the perftest and
# prod runs from clobbering each other.
frontend/.lighthouseci/
frontend/.lighthouseci.perftest/
frontend/.lighthouseci.prod/

# Python venv (per-developer, never committed)
python/.venv/
python/.pytest_cache/
*/.pytest_cache/